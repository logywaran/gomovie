spring:
  application:
    name: gomovie

  datasource:
    url: jdbc:mysql://localhost:3306/gomovie
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false